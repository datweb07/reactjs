### 1. Destructuring với mảng

- **Truy xuất phần tử theo chỉ số (index) thông thường:**
  - Muốn lấy các phần tử ở vị trí 0, 1, 2 từ mảng `username = ["Toàn", "Hà", "Huy"]`, lập trình viên cần khai báo lần lượt từng biến rời rạc.
  - Ví dụ: 

  ```javascript
    const user1 = username[0];
    const user2 = username[1];
    const user3 = username[2];
  ```

  - Cách làm này gây tốn nhiều dòng mã nguồn không cần thiết nếu mảng có nhiều phần tử.

- **Sử dụng Destructuring để gán trực tiếp:**
  - Cú pháp:

  ```javascript
    const [user4, user5, user6] = username;
  ```

  - Hệ thống tự động gán các giá trị tương ứng theo thứ tự chỉ số index 0, 1, 2.
  - Viết ngắn gọn chỉ trong **một dòng** duy nhất thay vì phải khai báo nhiều biến độc lập.

- **Bỏ qua phần tử không cần thiết:**
  - Sử dụng dấu phẩy `,` để bỏ qua phần tử ở vị trí bất kỳ mà bạn không muốn lấy dữ liệu.
  - Ví dụ lấy phần tử đầu và cuối, bỏ qua phần tử ở giữa:

  ```javascript
    const [user7, , user8] = username;
  ```

  - Các phần tử bị bỏ trống giữa hai dấu phẩy sẽ không được gán vào bất kỳ biến nào.

- **Destructuring với giá trị mặc định:**
  - Khi số lượng biến khai báo nhiều hơn số lượng phần tử thực tế có trong mảng, các biến vượt quá sẽ nhận giá trị `undefined`.
  - Có thể gán giá trị mặc định ngay khi khai báo để tránh lỗi nhận giá trị `undefined`.
  - Ví dụ:

  ```javascript
    const [u9, u10, u11, u12 = "No name"] = username;
  ```

  - Nếu vị trí tương ứng của biến `u12` không tồn tại giá trị trong mảng gốc, nó sẽ tự động nhận giá trị chuỗi `"No name"`.

- **Rest Parameter (Tham số còn lại) với Destructuring mảng:**
  - Sử dụng cú pháp dấu ba chấm `...` để gom toàn bộ các phần tử còn lại của mảng gốc vào một mảng mới độc lập.
  - Ví dụ:
  
  ```javascript
    const [user13, ...abc] = username;
  ```
  - Trong đó, biến `user13` lấy giá trị của phần tử đầu tiên, còn `abc` là một mảng mới chứa tất cả các phần tử còn lại.
  - Nếu mảng gốc không còn phần tử nào sót lại, `abc` sẽ trả về một mảng rỗng `[]`.

### 2. Destructuring với đối tượng (Object)

- **Truy xuất thuộc tính thông thường:**
  - Gán giá trị từng thuộc tính của đối tượng cho các biến một cách thủ công:
    ```javascript
    const name = user.name;
    const age = user.age;
    ```
  - Cách làm này làm tăng số lượng dòng code đáng kể khi đối tượng có cấu trúc phức tạp và chứa nhiều thuộc tính.

- **Sử dụng Destructuring đối tượng:**
  - Cú pháp:
    ```javascript
    const { name, age } = user;
    ```
  - **Lưu ý quan trọng:** Tên các biến được khai báo phải **đồng bộ chính xác với tên trường (key)** nằm trong đối tượng.
  - Ví dụ:
    ```javascript
    const user = { name: "Toàn", a: 20 };
    const { name, a } = user;
    ```
  - Kết quả nhận được: `name = "Toàn"`, `a = 20`.

- **Alias (Đổi tên / Bí danh) trong Destructuring:**
  - Khi bạn muốn đặt tên biến mới khác hoàn toàn với tên key mặc định của đối tượng, sử dụng cú pháp dấu hai chấm `:`.
    ```javascript
    const { name: ten1, a: tuoi1 } = user;
    ```
  - Lúc này, hai biến mới được sinh ra là `ten1` và `tuoi1` sẽ nhận giá trị tương ứng từ key `name` và `a`.

- **Giá trị mặc định trong Destructuring đối tượng:**
  - Tương tự mảng, nếu thuộc tính được khai báo không tồn tại trong đối tượng, biến sẽ nhận giá trị `undefined`.
  - Định nghĩa giá trị mặc định bằng dấu bằng `=` để phòng ngừa lỗi.
  - Ví dụ:
    ```javascript
    const { name: ten2, a: tuoi2, gender = "Male" } = user;
    ```
  - Nếu trường `gender` không có trong đối tượng `user`, biến `gender` sẽ nhận giá trị mặc định là `"Male"`.

- **Rest Parameter (Tham số còn lại) trong Destructuring đối tượng:**
  - Cú pháp:
    ```javascript
    const { name: ten3, ...rest } = user;
    ```
  - Biến `ten3` sẽ trích xuất và lấy giá trị của trường `name`.
  - Biến `rest` lúc này đóng vai trò là một đối tượng mới chứa toàn bộ các cặp key:value còn lại của đối tượng gốc.
  - Nếu tất cả thuộc tính đều đã được các biến trước đó trích xuất hết, `rest` sẽ trả về một đối tượng rỗng `{}`.

### 3. Ưu điểm của Destructuring

- **Tối ưu hóa mã nguồn:** Giảm thiểu số lượng dòng code một cách đáng kể khi cần bóc tách nhiều giá trị cùng lúc.
- **Tăng tính tường minh:** Giúp cấu trúc mã nguồn trở nên gọn gàng, dễ đọc và dễ bảo trì (đặc biệt hữu ích và được dùng làm chuẩn mực trong các thư viện/framework như React, Vue, Angular...).
- **Linh hoạt xử lý dữ liệu:** Hỗ trợ bỏ qua các phần tử không cần thiết, tự động gán giá trị mặc định để tránh lỗi hệ thống, và gom nhóm các dữ liệu thừa bằng Rest Parameter.

### Bảng so sánh tóm tắt phương pháp Destructuring giữa mảng và đối tượng

| Đặc điểm | Đối với Mảng (Array) | Đối với Đối tượng (Object) |
| :--- | :--- | :--- |
| **Cơ chế trích xuất** | Dựa theo chỉ số thứ tự vị trí (**index**). | Dựa theo tên định danh của thuộc tính (**key**). |
| **Cú pháp cơ bản** | `const [a, b, c] = arr;` | `const { key1, key2 } = obj;` |
| **Bỏ qua phần tử** | Dùng dấu phẩy để trống (ví dụ: `[a, , c]`). | Không áp dụng (bắt buộc phải chỉ định chính xác tên key). |
| **Giá trị mặc định** | `const [a, b = default] = arr;` | `const { key = default } = obj;` |
| **Tham số còn lại (Rest)** | `const [a, ...rest] = arr;` (Trả về một mảng mới). | `const { key, ...rest } = obj;` (Trả về một object mới). |
| **Alias (Đổi tên biến)** | Không cần thiết (vì trích xuất theo vị trí thứ tự). | Sử dụng cú pháp `{ key: alias }` để định nghĩa tên biến mới. |

### Kết luận chính

- **Destructuring là một kỹ thuật cốt lõi và cực kỳ quan trọng** trong JavaScript hiện đại (ES6+), giúp tối giản hóa cách thức tương tác và bóc tách dữ liệu.
- Việc hiểu và áp dụng thành thạo kỹ thuật này là **bắt buộc đối với lập trình viên** nhằm nâng cao hiệu suất làm việc, chuẩn bị nền tảng tốt trước khi học các kiến thức nâng cao về State, Props trong React.
- Hãy luôn lưu ý **bản chất khác biệt** của hai loại cấu trúc dữ liệu: mảng xử lý theo vị trí index tăng dần, còn đối tượng xử lý nghiêm ngặt theo tên key để tránh các lỗi logic khi triển khai Destructuring.
