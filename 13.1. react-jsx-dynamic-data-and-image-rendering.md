# Bài 13.1: ReactJS - Render Hình Ảnh, Dữ Liệu Động - Cách Sử Dụng JSX để Hiển Thị Dữ Liệu Linh Hoạt

Trong bài học này, chúng ta sẽ tìm hiểu cách sử dụng cú pháp JSX để đưa các dữ liệu động (biến, biểu thức, hàm, ngày giờ thực tế) lên giao diện thay vì sử dụng các chuỗi ký tự (text) tĩnh như HTML thuần.

## 1. Cơ chế hiển thị dữ liệu động với cặp ngoặc nhọn `{}`

Quy tắc cốt lõi của JSX: Để đưa một biến, một thuộc tính của đối tượng, một phép tính hoặc kết quả của một hàm JavaScript lên giao diện, chúng ta đặt chúng vào trong **cặp dấu ngoặc nhọn `{}**`.

### Ví dụ 1: Hiển thị Ngày và Giờ thực tế

Chúng ta sử dụng đối tượng `Date` của JavaScript để lấy ngày và giờ hiện tại, sau đó truyền vào JSX thông qua cặp ngoặc nhọn.

```jsx
function App() {
  // Logic JavaScript để lấy ngày giờ (Kiến thức nền tảng JS)
  const today = new Date().toLocaleDateString();
  const time = new Date().toLocaleTimeString();

  return (
    <>
      <p>
        Hôm nay là: <strong>{today}</strong>
      </p>
      <p>
        Thời gian hiện tại: <strong>{time}</strong>
      </p>
    </>
  );
}

```

*Cơ chế hoạt động:* Mỗi lần bạn nhấn F5 để tải lại trang, hàm Component sẽ được thực thi lại, tạo ra giá trị mới cho `today` và `time`, từ đó giao diện sẽ cập nhật thời gian thực tế một cách chính xác.

## 2. Bài toán: Xử lý Render nội dung ngẫu nhiên (Random)

### Yêu cầu bài toán

Giả sử ta có một mảng chứa 3 lời chào mừng khác nhau. Yêu cầu thiết lập sao cho mỗi lần người dùng F5 trình duyệt, trang web sẽ hiển thị ngẫu nhiên một trong ba câu chào này tại thẻ tiêu đề `<h1>`.

```javascript
const welcomeTexts = [
  "Chào mừng bạn đã đến với kênh tự học!",
  "Chúc bạn một ngày học tập ReactJS hiệu quả!",
  "Hôm nay chúng ta sẽ học cách render dữ liệu động."
];

```

### Giải pháp và Tối ưu hóa cấu trúc Code

Để code sạch sẽ và dễ quản lý, thay vì viết dồn tất cả logic phức tạp vào Component `App`, chúng ta sẽ tách nhỏ ra:

1. Tạo một hàm JavaScript thông thường phụ trách việc lấy chỉ số random (`0`, `1`, hoặc `2`).
2. Tạo một Component tùy chỉnh riêng tên là `Header` để xử lý giao diện hiển thị.

```jsx
import "./app.css";

// 1. Mảng chứa dữ liệu tĩnh
const welcomeTexts = [
  "Chào mừng bạn đã đến với kênh tự học!",
  "Chúc bạn một ngày học tập ReactJS hiệu quả!",
  "Hôm nay chúng ta sẽ học cách render dữ liệu động."
];

// 2. Hàm JS thuần để lấy chỉ số ngẫu nhiên (Khai báo dạng camelCase)
function getRandomIndex() {
  return Math.floor(Math.random() * welcomeTexts.length);
}

// 3. Component tùy chỉnh Header (Viết hoa chữ cái đầu)
function Header() {
  // Lấy dữ liệu động dựa trên chỉ số ngẫu nhiên
  const randomIndex = getRandomIndex();
  const title = welcomeTexts[randomIndex];
  
  const today = new Date().toLocaleDateString();
  const time = new Date().toLocaleTimeString();

  return (
    <div className="header-container">
      {/* Đưa biến title vào trong h1 */}
      <h1>{title}</h1> 
      <p>
        Hôm nay là: <strong>{today}</strong> - Giờ hiện tại: <strong>{time}</strong>
      </p>
    </div>
  );
}

// 4. Component gốc App
function App() {
  return (
    <>
      {/* Gọi Component Header thông qua thẻ tự đóng */}
      <Header />
    </>
  );
}

export default App;

```

## 3. Một số lưu ý về Style (CSS) trong dự án Vite

* Khi khởi tạo dự án bằng Vite, file `app.css` sẽ có sẵn rất nhiều đoạn mã mặc định của hệ thống. Bạn có thể xóa bớt các cấu hình thừa và chỉ giữ lại định dạng cho phân vùng `#root` (tác động trực tiếp lên thẻ div gốc có `id="root"` trong file `index.html`) để tùy biến giao diện theo ý mình.
* Khi viết class cho thẻ HTML trong ReactJS (JSX), chúng ta không dùng thuộc tính `class="..."` như thông thường mà bắt buộc phải đổi tên thành **`className="..."`** (vì từ khóa `class` đã bị trùng với khái niệm Class trong lập trình hướng đối tượng của JavaScript).
