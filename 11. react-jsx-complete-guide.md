# Bài 11: ReactJS - Cách Sử Dụng JSX Trong React 

## 1. Khái niệm về Component trong React

### Nhược điểm của cách viết code thuần (HTML/CSS/JS truyền thống)

Khi phát triển một trang web theo cách truyền thống, nếu có các khối giao diện (item) lặp đi lặp lại có cấu trúc giống hệt nhau (chỉ khác nhau về nội dung văn bản hoặc hình ảnh) thì bạn buộc phải copy-paste đoạn code đó ra nhiều lần.

* **Ví dụ:** Một danh sách gồm 5 khối item hiển thị thông tin nhân viên (gồm hình ảnh, tên, chức danh). Khi rê chuột vào ảnh thì ảnh phóng to.
* **Hệ quả:**
* Gây lặp code rất nhiều (duplicate code).
* Dự án lớn sẽ cực kỳ khó quản lý, phải viết chú thích (`comment`) liên tục để phân biệt.
* Khi cần thay đổi cấu trúc giao diện, bạn phải chỉnh sửa ở cả 5 vị trí. Nếu dùng tính năng tìm kiếm thay thế (`Ctrl + H`) thì rất dễ xảy ra sai sót hoặc sửa nhầm mà không phát hiện được.



### Ưu điểm khi sử dụng React Component

Trong React, **Component** là các khối mã độc lập chứa giao diện (HTML), kiểu dáng (CSS) và logic xử lý (JavaScript) liên quan đến một phần của giao diện.

* **Tính tái sử dụng cao:** Bạn chỉ cần xây dựng một khối giao diện tiêu chuẩn duy nhất. Sau đó, gọi tên Component đó ra nhiều lần ở nơi cần hiển thị.
* **Quản lý dễ dàng:** Giúp chia nhỏ giao diện phức tạp thành các phần nhỏ hơn để dễ kiểm soát.
* **Cập nhật nhanh chóng:** Khi cần sửa đổi cấu trúc hoặc logic, bạn chỉ cần chỉnh sửa tại một nơi (chính Component đó), tất cả những nơi đang tái sử dụng nó trên trang web sẽ tự động cập nhật theo.

## 2. Khởi tạo dự án React bằng Vite

Các bước tạo nhanh một dự án React mới bằng công cụ Vite thông qua Terminal (`Ctrl + ~` hoặc `Ctrl + bactic`):

1. **Chạy lệnh khởi tạo:**
`npm create vite@latest`
2. **Thiết lập dự án:**
* Nhập tên Project (Ví dụ: `jsx`).
* Chọn Framework: Di chuyển mũi tên xuống dòng thứ 3 chọn **`react`**.
* Chọn Variant: Chọn **`JavaScript`**.


3. **Di chuyển vào thư mục dự án vừa tạo:**
`cd tên_project` (Ví dụ: `cd jsx`).
4. **Cài đặt các gói thư viện phụ thuộc:**
`npm install`
5. **Chạy dự án ở môi trường Local (Phát triển):**
`npm run dev`
*Mở trình duyệt và truy cập vào đường dẫn Local được Vite cấp (bằng cách nhấn `Ctrl + Click` vào link).*

> **Mẹo nhỏ:** Để tránh nhầm lẫn cây thư mục khi mở nhiều dự án, bạn có thể click chuột phải vào thư mục dự án hiện tại, chọn mở hẳn thư mục đó ở một cửa sổ VS Code độc lập (`Ctrl + K + O`).

## 3. Cấu trúc thư mục và Khái niệm JSX

### Cấu trúc file chạy cơ bản

* **`index.html`:** File HTML gốc của ứng dụng. Trong thẻ `<body>` hoàn toàn không có nội dung giao diện nào khác ngoài một thẻ duy nhất: `<div id="root"></div>`.
* **`src/main.jsx`:** File này sẽ tìm phần tử có `id="root"` trong `index.html` và sử dụng phương thức `createRoot().render()` để đẩy toàn bộ giao diện từ Component gốc (thường là `<App/>`) vào trong thẻ div đó.
* **`src/App.jsx`:** Nơi chứa giao diện và code của Component chính, hiển thị trực tiếp lên trình duyệt.

### JSX là gì?

Khi mở file `App.jsx`, bạn sẽ thấy một hàm JavaScript cơ bản nhưng lại trả về (`Return`) các thẻ HTML:

```jsx
function App() {
  return (
    <div>
      <h1>Xin chào các bạn!</h1>
    </div>
  );
}

```

* **Định nghĩa:** **JSX** viết tắt của **JavaScript Syntax Extension** (Cú pháp mở rộng của JavaScript). Nó cho phép chúng ta viết trực tiếp mã HTML ngay bên trong mã JavaScript.
* **Cơ chế hoạt động:** Trình duyệt web thông thường không thể đọc và hiểu trực tiếp file `.jsx`. Do đó, ứng dụng cần một công cụ thông dịch để biên dịch JSX về mã JavaScript thuần. Trong dự án này, **Vite** đảm nhận vai trò chuyển đổi đó, chạy trên môi trường **Node.js**.

## 4. Cú pháp Fragment trong React

Theo nguyên tắc cốt lõi của React, một Component chỉ được phép `Return` về **một phần tử bọc ngoài duy nhất**.

Nếu giao diện của bạn có nhiều thẻ nằm song song (ví dụ thẻ `<h1>` và thẻ `<p>`), cách làm cũ là phải dùng một thẻ `<div>` để bọc chúng lại:

```jsx
// Cách dùng thẻ <div> bọc (gây dư thừa thẻ)
return (
  <div>
    <h1>Tiêu đề</h1>
    <p>Nội dung đoạn văn</p>
  </div>
);

```

### Fragment giải quyết điều gì?

Việc thêm thẻ `<div>` chỉ với mục đích bao bọc sẽ làm cấu trúc cây thư mục DOM của trang web bị thừa thãi, bẩn và không cần thiết. Để giải quyết, React cung cấp cú pháp **Fragment** (sử dụng cặp thẻ đóng mở trống, không có tên):

```jsx
// Sử dụng Fragment trong React
return (
  <>
    <h1>Tiêu đề</h1>
    <p>Nội dung đoạn văn</p>
  </>
);

```

* **Ưu điểm:** Fragment giúp bao bọc các phần tử song song đúng theo quy tắc của React mà không sinh ra bất kỳ thẻ HTML dư thừa nào trên cây DOM khi render, giữ cho cấu trúc HTML của trang web luôn sạch sẽ.
* Để tìm hiểu sâu hơn, bạn có thể tham khảo tài liệu chính thức tại trang chủ: `react.dev` với từ khóa **Fragment**.
