# Bài 10: Review Javascript Spread Operator - Làm việc với mảng và object trong JS bằng Spread Operator

Toán tử **Spread** (Spread Operator) trong JavaScript sử dụng cú pháp **ba dấu chấm (`...`)**.
Tác dụng cốt lõi của toán tử này là **"trải" (hoặc giải)** các phần tử của một mảng ra thành các phần tử riêng lẻ, hoặc "trải" các cặp key-value của một đối tượng (object) ra để sao chép, kết hợp hoặc gộp dữ liệu một cách linh hoạt.

## 1. Làm việc với Mảng (Array) bằng Spread Operator

Toán tử Spread giúp gộp, sao chép hoặc thêm phần tử mới vào mảng cũ một cách nhanh chóng mà không làm thay đổi trực tiếp mảng gốc (giữ tính bất biến - immutability).

### Ví dụ 1: Thêm phần tử và sao chép mảng

Giả sử ta có một mảng sở thích ban đầu và muốn thêm các giá trị mới vào:

```javascript
// Khai báo mảng ban đầu
let soThich = ["đọc sách", "nghe nhạc"];

// Một biến chứa giá trị muốn thêm
const soThichMoi = "đi du lịch";

// Tiến hành cập nhật bằng Spread Operator
soThich = [...soThich, "ABC", soThichMoi];

console.log(soThich);
// Kết quả Output: ["đọc sách", "nghe nhạc", "ABC", "đi du lịch"]

```

*Giải thích cơ chế:* Cú pháp `...soThich` sẽ bóc tách mảng gốc ra thành từng phần tử đơn lẻ là `"đọc sách"`, `"nghe nhạc"` rồi đặt vào mảng mới, sau đó gộp thêm phần tử `"ABC"` và `"đi du lịch"`.

### Ví dụ 2: Gộp hai mảng (Tránh lỗi mảng lồng nhau)

Nếu `soThichMoi` cũng là một mảng và bạn không dùng toán tử Spread, mảng sẽ bị lồng nhau (Array inside Array):

```javascript
let soThich = ["đọc sách", "nghe nhạc"];
const soThichMoi = ["đi du lịch", "xem phim"];

// Cách viết SAI (gây lồng mảng):
// soThich = [...soThich, "ABC", soThichMoi]; 
// Kết quả sẽ thành: ["đọc sách", "nghe nhạc", "ABC", ["đi du lịch", "xem phim"]]

// Cách viết ĐÚNG (gộp phẳng 2 mảng):
soThich = [...soThich, "ABC", ...soThichMoi];

console.log(soThich);
// Kết quả Output: ["đọc sách", "nghe nhạc", "ABC", "đi du lịch", "xem phim"]

```

## 2. Làm việc với Đối tượng (Object) bằng Spread Operator

Tương tự như mảng, đối với Object, toán tử Spread sẽ giải các cặp `key: value` ra để gộp hoặc cập nhật dữ liệu.

### Ví dụ 1: Gộp và cập nhật thông tin Object

```javascript
// Đối tượng user ban đầu
let user = {
    id: 1,
    name: "Nguyễn Văn A",
    email: "one@mail.com"
};

// Thông tin cần cập nhật
const updateInfo = {
    address: "123 đường ABC",
    role: "admin"
};

// Gộp dữ liệu bằng cách rải cả hai Object vào một Object mới
user = { ...user, ...updateInfo };

console.log(user);
/* Kết quả Output:
{
    id: 1,
    name: "Nguyễn Văn A",
    email: "one@mail.com",
    address: "123 đường ABC",
    role: "admin"
}
*Lưu ý: Thứ tự các key hiển thị trong tab Console có thể tự động sắp xếp lại theo bảng chữ cái.
*/

```

### Ví dụ 2: Quy tắc ghi đè (Override) thuộc tính trùng nhau

Khi gộp các object bằng toán tử Spread, nếu xảy ra tình trạng trùng `key` giữa các đối tượng, **đối tượng nào nằm ở phía sau (đứng sau) sẽ ghi đè giá trị lên đối tượng đứng trước**.

```javascript
const updateInfo = {
    address: "123 đường ABC",
    role: "admin" // role ở đây là admin
};

let user = {
    id: 1,
    name: "Nguyễn Văn A",
    email: "one@mail.com",
    role: "member" // role ở đây là member
};

// user đứng sau updateInfo
user = { ...updateInfo, ...user };

console.log(user);
/* Kết quả Output:
{
    address: "123 đường ABC",
    role: "member",   <-- Giá trị 'member' của user đứng sau đã ghi đè lên 'admin'
    id: 1,
    name: "Nguyễn Văn A",
    email: "one@mail.com"
}
*/

```

## Tổng kết

* Toán tử Spread (`...`) giúp bóc tách cấu trúc mảng hoặc object một cách nhanh chóng.
* Cực kỳ hữu ích trong việc sao chép dữ liệu cũ, gộp dữ liệu từ nhiều nguồn mà không làm thay đổi trực tiếp (mutate) biến gốc.
* Quy tắc ưu tiên đối với Object: Thuộc tính nào khai báo hoặc rải (`...`) **phía sau** sẽ ghi đè (overwrite) thuộc tính trùng tên ở **phía trước**.
* Đây là nền tảng cốt lõi được ứng dụng rất nhiều trong ReactJS khi xử lý và cập nhật `State`.
