# [React JS] Render Hình Ảnh & Dữ Liệu Động Trong JSX

## 1. Đặt vấn đề & Khởi tạo Component mới

Trong bài học trước, chúng ta đã biết cách khai báo các biến cơ bản (ngày tháng, thời gian) và render chúng lên giao diện bằng cặp dấu ngoặc nhọn `{}`.

Để tìm hiểu sâu hơn về những loại dữ liệu mà cặp dấu `{}` có thể xử lý, chúng ta sẽ tạo một hàm (Component) mới có tên là `DynamicValue`.

*Lưu ý: Theo quy tắc của React, tên Component phải viết hoa chữ cái đầu tiên.*

```jsx
import React from 'react';

function DynamicValue() {
    // Khai báo biến và logic sẽ nằm ở đây
    
    return (
        <>
            <h2>Chào ...</h2>
        </>
    );
}

// Gọi sử dụng inside App component:
// <DynamicValue />

```

## 2. Các loại dữ liệu có thể Render trong dấu ngoặc nhọn `{}`

### a. Chèn Biến (Variables)

Bạn có thể khai báo một biến chứa chuỗi hoặc số và truyền trực tiếp vào JSX.

```jsx
const name = "Nam";

return (
    <>
        <h2>Chào {name}</h2> 
        {/* Kết quả hiển thị: Chào Nam */}
    </>
);

```

### b. Chèn Biểu thức JavaScript (Expressions)

Cặp dấu `{}` cho phép tính toán trực tiếp các biểu thức toán học hoặc logic. Biểu thức sẽ được xử lý trước khi render ra giao diện.

```jsx
return (
    <>
        <p>Kết quả: {1 + 2}</p> {/* Hiển thị: Kết quả: 3 */}
        <p>Tính toán phức tạp: {1 + 2 * 5}</p> {/* Hiển thị: Kết quả: 11 (Nhân chia trước cộng trừ sau) */}
    </>
);

```

> **CẢNH BÁO QUAN TRỌNG:** Bạn **KHÔNG THỂ** chèn câu lệnh (Statements) như câu lệnh điều kiện `if-else`, vòng lặp `for`, `while` trực tiếp vào trong `{}`. Nếu viết như dưới đây, hệ thống sẽ báo lỗi đỏ và không chạy được:
> ```jsx
> {/* ĐOẠN CODE NÀY SẼ BỊ LỖI */}
> { if (true) { return "Đúng rồi" } } 
> 
> ```
> 
> 

### c. Sử dụng Toán tử ba ngôi (Ternary Operator) thay thế cho `If-Else`

Vì không thể dùng câu lệnh `if-else` truyền thống bên trong JSX, chúng ta sẽ thay thế bằng toán tử ba ngôi để thực hiện render có điều kiện (Conditional Rendering).

```jsx
const isLoggedIn = true; // hoặc false

return (
    <>
        <p>
            {isLoggedIn ? "Bạn đã đăng nhập" : "Hãy đăng nhập"}
        </p>
    </>
);

```

* **Nếu `isLoggedIn = true`:** Hiển thị "Bạn đã đăng nhập" (kết quả sau dấu `?`).
* **Nếu `isLoggedIn = false`:** Hiển thị "Hãy đăng nhập" (kết quả sau dấu `:`).

### d. Chèn Thuộc tính của Đối tượng (Object Properties)

Bạn có thể truy cập và hiển thị các thuộc tính của một Object bằng cú pháp dấu chấm (`object.property`).

```jsx
const person = {
    name: "Lan",
    age: 38
};

return (
    <>
        <p>Tên: {person.name}, Tuổi: {person.age}</p>
        {/* Hiển thị: Tên: Lan, Tuổi: 38 */}
    </>
);

```

## 3. Cách Render Hình ảnh trong React (Thư mục `src`)

Để quản lý và hiển thị hình ảnh trong React từ thư mục mã nguồn (`src`), chúng ta không truyền đường dẫn dạng chuỗi thuần túy vào thuộc tính `src` của thẻ `<img>` mà phải thông qua việc `import`.

### Các bước thực hiện:

1. Tạo một thư mục chứa ảnh có tên là `img` nằm bên trong thư mục `assets` (hoặc cấu trúc tương đương thuộc `src`).
2. Copy bức ảnh cần hiển thị vào (ví dụ: `demo.jpg`).
3. Thực hiện `import` bức ảnh ở đầu file component như một module và truyền biến đó vào thẻ `<img />`.

```jsx
// 1. Import hình ảnh bằng đường dẫn tương đối (Relative Path)
import demoImg from '../../assets/img/demo.jpg'; 

function DynamicValue() {
    return (
        <>
            {/* 2. Truyền biến hình ảnh vào thuộc tính src qua cặp dấu ngoặc nhọn */}
            <img src={demoImg} alt="Ảnh demo" />
        </>
    );
}

```

> **Mẹo nhỏ trong VS Code:** Khi paste đường dẫn hệ điều hành Windows thường bị ngược dấu xược (`\`), bạn có thể bôi đen dấu xược đó rồi nhấn tổ hợp phím `Ctrl + Shift + L` để chọn tất cả và đổi nhanh thành dấu xược xuôi (`/`) hợp lệ cho code.
