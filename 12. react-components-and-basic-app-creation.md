# Bài 12: ReactJS - Component trong React - Tạo ứng dụng React cơ bản 

## 1. Bản chất của một React Component

Về mặt bản chất, một Component trong React thực ra chính là **một hàm JavaScript thông thường** (Function). Tuy nhiên, hàm này bắt buộc phải tuân thủ nghiêm ngặt hai quy tắc cốt lõi của React:

1. **Tên hàm phải viết hoa chữ cái đầu tiên:** * Ví dụ: `function App()`, `function Header()`.
* *Lý do:* Để React có thể phân biệt được đâu là một thẻ tự tùy chỉnh (Custom Component) do lập trình viên định nghĩa, đâu là các thẻ HTML có sẵn của trình duyệt (như `header`, `div`, `p`, `h1` luôn viết thường).


2. **Hàm phải trả về (`Return`) một giá trị có thể hiển thị được:** * Thường giá trị trả về là mã HTML (JSX).
* Giá trị trả về phải nằm gọn trong **một phần tử bọc ngoài duy nhất**. Đó là lý do tại sao ta thường dùng cặp thẻ trống `<></>` (**Fragment**) hoặc thẻ `<div>` để bọc toàn bộ nội dung bên trong lại. Nếu trả về nhiều thẻ nằm song song mà không có thẻ bọc ngoài, chương trình sẽ báo lỗi ngay lập tức.

## 2. Thực hành tạo và tái sử dụng Component mới

Bạn có thể tạo ra nhiều Component khác nhau trong cùng một file (hoặc tách file) và gọi chúng lồng vào nhau.

### Bước 1: Khai báo Component `Header`

Ngay phía trên hoặc phía dưới hàm `App()`, ta có thể tự định nghĩa thêm một Component mới tên là `Header`:

```jsx
function Header() {
  return (
    <>
      <h2>Chào mừng đến với thế giới React!</h2>
      <p>
        Hôm nay là <strong>22/11/2024</strong>. 
        Thời gian hiện tại: <strong>19:00</strong>
      </p>
    </>
  );
}

```

### Bước 2: Gọi và tái sử dụng Component `Header` trong `App`

Để hiển thị nội dung của `Header` lên trang web, ta gọi tên nó bên trong phần mã JSX của Component `App` giống như một thẻ HTML. Bạn có thể viết theo 2 cách:

* Cách viết đầy đủ: `<Header></Header>`
* Cách viết ngắn gọn (Thẻ tự đóng - Self-closing tag): `<Header />`

```jsx
function App() {
  return (
    <>
      {/* Gọi Component Header nhiều lần */}
      <Header />
      <Header />
      <Header />
      
      <hr />
      <h1>Đây là nội dung chính của App</h1>
    </>
  );
}

export default App;

```

* **Hiệu quả:** Khi gọi `<Header />` ba lần, nội dung của Header sẽ hiển thị lặp lại 3 lần trên trình duyệt. Bạn hoàn toàn không cần phải copy-paste lại đống mã HTML phức tạp kia, giúp code ngắn gọn và dễ bảo trì hơn rất nhiều.

## 3. So sánh trực quan: JavaScript thuần vs ReactJS

Để tạo ra một cấu trúc giao diện gồm một tiêu đề `<h1>`, một danh sách `<ul>` kèm các thẻ `<li>`, và một đoạn văn `<p>` vào trong thẻ `<div id="root"></div>`, mã nguồn của hai cách viết sẽ khác nhau như sau:

### Cách 1: Sử dụng JavaScript thuần (Cực kỳ dài dòng và khó đọc)

```javascript
const root = document.getElementById("root");

// Tạo thẻ h1 và thêm nội dung
const h1 = document.createElement("h1");
h1.innerText = "Danh sách bài học";

// Tạo thẻ ul và các thẻ li
const ul = document.createElement("ul");
const li1 = document.createElement("li");
li1.innerText = "Bài 1: Khởi tạo";
const li2 = document.createElement("li");
li2.innerText = "Bài 2: Component";
ul.appendChild(li1);
ul.appendChild(li2);

// Tạo thẻ p
const p = document.createElement("p");
p.innerText = "Chúc các bạn học tốt!";

// Đẩy tất cả vào nút gốc root
root.appendChild(h1);
root.appendChild(ul);
root.appendChild(p);

```

### Cách 2: Sử dụng ReactJS (Trực quan, ngắn gọn, dễ bảo trì)

```jsx
function App() {
  return (
    <>
      <h1>Danh sách bài học</h1>
      <ul>
        <li>Bài 1: Khởi tạo</li>
        <li>Bài 2: Component</li>
      </ul>
      <p>Chúc các bạn học tốt!</p>
    </>
  );
}

```

> **Nhận xét:** Nhìn vào mã ReactJS, cấu trúc giao diện hiện ra vô cùng trực quan và dễ đọc vì nó giống hệt mã HTML thông thường, không cần phải thực hiện từng bước tạo thẻ, gán text, rồi bọc lồng (`appendChild`) phức tạp như JS thuần. Đây là lý do vì sao các dự án lớn đều ưu tiên sử dụng React.
